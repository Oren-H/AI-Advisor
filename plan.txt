
meta data fix
    times need to be seperated (start time, end time, days) - store that time as military time 
    enrollment needs to be seperated - enrolled + max enrollment 






Fix the scrapper 

RAG? 

Database of Classes 
    Make into relational/graph/vector DB and use SQL language to access data? 
    Parent document retrieval 
    RAG on a bunch of parent documents (physics 101 is the children document)
    Hierarchical RAG (chop classes that are outside departments)
    Meta data hierarchy (?) 
    React Agent (reason and react; vector search tool, web search tool)
        Langgraph/Langchain react agent package 
            Catalog look up - vector search - parent document retrieval 

Tevilli Crawl 

User data 
    Major/minor/interests/career 
    Classes you have taken 

Context
    DB of classes 
    User preferences    




To-do

🔧 PHASE 1: Data Collection & Cleaning
1.1 Prep Raw Data

 - Clean scraped CSV into structured columns (course code, title, dept, time, location)
 - Parse class times into structured JSON:
   → "Mon/Wed 10:10-11:25" → [{day: "Mon", start: "10:10", end: "11:25"}, …]
 - Normalize course codes (COMS W1004 → COMS1004)
 - Tag course level (1000, 2000, 3000+) for filtering
 - Remove/handle empty descriptions

1.2 Scrape Additional Data

 - ✅ Grad School classes (if applicable)
 - [ ] CULPA professor reviews
 - [ ] Major/minor/track requirements (for roadmap + constraints)

🗄️ PHASE 2: Storage and Querying
2.1 PostgreSQL Setup

 - Create schema with:
   • courses (course_id, dept, title, description, credits, level, etc.)
   • sections (course_id, section, instructor, timeslot, location)
   • reviews (professor, course_id, review, tags)
   • requirements (major, requirement_type, course_list)
 - Use pgvector extension if you want vector embeddings in-database
 - Ingest cleaned data from CSV into SQL

2.2 Create SQL-based Query Layer

 - get_courses_by_filters(department, level, day_range, ...)
 - check_schedule_conflicts(courses, user_availability)
 - get_prereqs(course_id)
 - get_requirements(major_or_minor)

📚 PHASE 3: RAG Document Prep
3.1 Create Parent/Child Docs

 - Each course = parent document
 - Split long description, weekly topics, CULPA quotes into child chunks
 - Add metadata:
   {
     "course_id": "COMS3261",
     "department": "COMS",
     "level": 3000,
     "instructor": "Nina Narodytska",
     "tags": ["theory", "algorithms"],
     "times": [{"day": "Mon", "start": "10:10", "end": "11:25"}]
   }

3.2 Vector Indexing

 - Use OpenAI or HuggingFace embeddings
 - Store in Chroma or pgvector
 - Test retrieval using LangChain’s ParentDocumentRetriever

🧠 PHASE 4: Tools
4.1 Tool: SearchCatalog

 - Takes a department + query string
 - Does semantic search within that department only
 - Returns ranked course results (title, description, meeting time)

4.2 Tool: CheckSchedule

 - Takes a list of courses and user constraints
 - Verifies no time conflicts
 - (Optional) Scores schedule based on preferences (e.g., back-to-back)

4.3 Tool: FetchRequirements

 - For a given course, return prereqs/coreqs (parsed from text)
 - For a given major/minor, return structured distribution requirements

4.4 Tool: SuggestRoadmap

 - Given: major + courses taken
 - Output: list of eligible courses and possible paths forward

🧠 PHASE 5: LangGraph ReAct Agent
5.1 Define Agent Tools

 - Register the tools above as LangChain Tool objects
 - Give clear description fields so the LLM uses the right one

5.2 LangGraph Setup

 - Define state object:
   {
     "major": "CS",
     "taken_classes": ["COMS1004"],
     "preferences": {
       "no_early_classes": True,
       "prefer_back_to_back": False,
       "avoid_friday": True
     }
   }
 - Build LangGraph using create_react_agent()
 - Allow state to update across steps (e.g., user adds preferences mid-convo)



